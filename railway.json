{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile",
    "useNixpacks": false
  },
  "deploy": {
    "startCommand": "cd /app/apps/backend && yarn db:migrate && yarn start",
    "healthcheckPath": "/health",
    "restartPolicy": "ON_FAILURE"
  }
}
